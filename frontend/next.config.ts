import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // NEXT_PUBLIC_API_URL is set in Vercel project settings (see docs/deployment-plan.md).
};

export default nextConfig;
